import datapoints from '../config/appData/datapoints.json';
import forms from '../config/appData/forms.json';
import prompts from '../config/appData/prompts.json';
import topBar from '../config/appData/topBar.json';

export default {
  appData: {},
  events: [],
  activeForm: {},
  activePrompts: [],
  activeFormName: "standby",
  datapoints,
  forms,
  prompts,
  topBar,
}
